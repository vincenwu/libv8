import("//build/toolchain/gcc_toolchain.gni")

clang_toolchain("clang-libc++") {
  extra_cxxflags = "-stdlib=libc++"
  extra_ldflags = "-stdlib=libc++"

  toolchain_args = {
    current_cpu = "x64"
    current_os = "linux"
    is_clang = true
  }
}
